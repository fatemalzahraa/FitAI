/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module app

(function(){

  // controller fitAI.accounts.account

  (function(){

    abp.utils.createNamespace(window, 'fitAI.accounts.account');

    fitAI.accounts.account.register = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/account/register',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller fitAI.ai.ai

  (function(){

    abp.utils.createNamespace(window, 'fitAI.ai.ai');

    fitAI.ai.ai.analyzeComment = function(yorumMetni, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ai/analyze-comment' + abp.utils.buildQueryString([{ name: 'yorumMetni', value: yorumMetni }]) + '',
        type: 'POST'
      }, ajaxParams));
    };

    fitAI.ai.ai.processPendingComments = function(magazaId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ai/process-pending-comments/' + magazaId + '',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

  })();

  // controller fitAI.analytics.analytics

  (function(){

    abp.utils.createNamespace(window, 'fitAI.analytics.analytics');

    fitAI.analytics.analytics.getStoreSummary = function(magazaId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/analytics/store-summary/' + magazaId + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.analytics.analytics.getSentimentDistribution = function(magazaId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/analytics/sentiment-distribution/' + magazaId + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.analytics.analytics.getTopThemes = function(magazaId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/analytics/top-themes/' + magazaId + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller fitAI.application.auth.auth

  (function(){

    abp.utils.createNamespace(window, 'fitAI.application.auth.auth');

    fitAI.application.auth.auth.register = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/auth/register',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.application.auth.auth.login = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/auth/login',
        type: 'POST',
        data: JSON.stringify(input)
      }, { dataType: 'text' }, ajaxParams));
    };

  })();

  // controller fitAI.controllers.auth

  (function(){

    abp.utils.createNamespace(window, 'fitAI.controllers.auth');

    fitAI.controllers.auth.register = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/auth/register',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.controllers.auth.login = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/auth/login',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller fitAI.magazalar.magaza

  (function(){

    abp.utils.createNamespace(window, 'fitAI.magazalar.magaza');

    fitAI.magazalar.magaza.getByEposta = function(eposta, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/magaza/by-eposta' + abp.utils.buildQueryString([{ name: 'eposta', value: eposta }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.magazalar.magaza['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/magaza/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    fitAI.magazalar.magaza.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/magaza',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.magazalar.magaza.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/magaza/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.magazalar.magaza.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/magaza/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.magazalar.magaza.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/magaza' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller fitAI.urunler.urun

  (function(){

    abp.utils.createNamespace(window, 'fitAI.urunler.urun');

    fitAI.urunler.urun.getListByMagazaId = function(magazaId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/urun/by-magaza-id/' + magazaId + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.urunler.urun.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/urun/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.urunler.urun['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/urun/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    fitAI.urunler.urun.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/urun',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.urunler.urun.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/urun/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.urunler.urun.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/urun' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller fitAI.yorumlar.yorum

  (function(){

    abp.utils.createNamespace(window, 'fitAI.yorumlar.yorum');

    fitAI.yorumlar.yorum.getListByUrunId = function(urunId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/yorum/by-urun-id/' + urunId + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.yorumlar.yorum.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/yorum',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.yorumlar.yorum.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/yorum/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    fitAI.yorumlar.yorum['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/yorum/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    fitAI.yorumlar.yorum.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/yorum/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    fitAI.yorumlar.yorum.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/yorum' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

})();


